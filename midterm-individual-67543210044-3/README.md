# Student Management System - Layered Architecture

## 📋 Project Information
- **Student Name:** ภานุวัฒน์ ต๋าคำ
- **Student ID:** 67543210044-3
- **Course:** ENGSE207 Software Architecture

## 🏗️ Architecture Style
Layered Architecture (3-tier)

## 📂 Project Structure
The project is organized into a layered architecture with the following main directories:

-   `layered-student/`: ประกอบด้วยแอปพลิเคชันแบ็กเอนด์ที่สร้างด้วยสถาปัตยกรรมแบบ Layered
    -   `public/`: ไฟล์ Assets แบบคงที่ (HTML, CSS, JS) สำหรับส่วนหน้าบ้าน (frontend)
    -   `src/`: ซอร์สโค้ดแบ็กเอนด์ ซึ่งแบ่งออกเป็น Layer ต่างๆ ดังนี้:
        -   `business/`: ประกอบด้วยตรรกะทางธุรกิจ (business logic), บริการ (services) และตัวตรวจสอบความถูกต้อง (validators)
            -   `services/`: จัดการการดำเนินงานทางธุรกิจและประสานงานการโต้ตอบข้อมูล
            -   `validators/`: ตรวจสอบความถูกต้องของข้อมูลขาเข้าตามกฎทางธุรกิจ
        -   `data/`: จัดการการเข้าถึงข้อมูลและการจัดเก็บถาวร
            -   `database/`: จัดการการเชื่อมต่อฐานข้อมูล
            -   `repositories/`: จัดเตรียม Layer นามธรรมสำหรับการจัดเก็บและเรียกค้นข้อมูล.
        -   `presentation/`: จัดการการโต้ตอบกับผู้ใช้, การกำหนดเส้นทาง (routing) และคอนโทรลเลอร์ (controllers)
            -   `controllers/`: ประมวลผลคำขอและจัดเตรียมการตอบกลับ
            -   `middlewares/`: การประมวลผลคำขอ/การตอบกลับทั่วทั้งระบบ (เช่น การจัดการข้อผิดพลาด)
            -   `routes/`: กำหนดปลายทาง API (API endpoints) และแมปไปยังคอนโทรลเลอร์

-   `monolithic-student/`: ประกอบด้วยแอปพลิเคชันแบบ Monolithic ดั้งเดิมสำหรับการเปรียบเทียบ

## 🎯 Refactoring Summary

### ปัญหาของ Monolithic (เดิม):
-   **การผูกมัดแน่นหนา (Tight Coupling):** ส่วนประกอบต่างๆ ของระบบมีการพึ่งพากันสูง ทำให้การเปลี่ยนแปลงในส่วนหนึ่งอาจส่งผลกระทบเป็นวงกว้าง
-   **ความยากในการปรับขนาด (Scalability Issues):** เมื่อความต้องการเพิ่มขึ้น ต้องทำการ Scale ทั้งระบบพร้อมกัน ไม่สามารถ Scale เฉพาะส่วนที่จำเป็นได้
-   **ความซับซ้อนในการบำรุงรักษา (Maintainability Challenges):** โค้ดเบสมีขนาดใหญ่และซับซ้อน ทำให้เข้าใจยาก แก้ไขยาก และเพิ่มฟีเจอร์ใหม่ๆ ได้ลำบาก
-   **ข้อจำกัดทางเทคโนโลยี (Technology Lock-in):** การนำเทคโนโลยีใหม่ๆ มาใช้ทำได้ยาก เนื่องจากอาจกระทบกับระบบโดยรวม
-   **กระบวนการ Deployment ที่ซับซ้อน (Deployment Complexity):** การเปลี่ยนแปลงเพียงเล็กน้อยก็จำเป็นต้อง Deploy ทั้งระบบใหม่ ทำให้กระบวนการปล่อยเวอร์ชันช้าลง

### วิธีแก้ไขด้วย Layered Architecture:
-   **ลดการผูกมัดแน่นหนา:** มีการแบ่งแยกความรับผิดชอบที่ชัดเจนระหว่าง Layer ต่างๆ ทำให้แต่ละ Layer พึ่งพากันน้อยลง การเปลี่ยนแปลงใน Layer หนึ่งไม่กระทบกับ Layer อื่นๆ โดยตรง
-   **ปรับปรุงความสามารถในการปรับขนาด:** แม้จะยังคงเป็นระบบรวมศูนย์ แต่การแบ่ง Layer ทำให้สามารถระบุและปรับปรุงประสิทธิภาพของแต่ละ Layer ได้ง่ายขึ้น และยังเป็นรากฐานที่ดีสำหรับการปรับไปใช้ Microservices ในอนาคต
-   **เพิ่มความง่ายในการบำรุงรักษา:** โค้ดถูกจัดระเบียบเป็น Layer ที่ชัดเจน ทำให้เข้าใจโครงสร้างของระบบได้ง่ายขึ้น ลดความซับซ้อนในการพัฒนา แก้ไข และบำรุงรักษา
-   **ลดข้อจำกัดทางเทคโนโลยี:** แต่ละ Layer สามารถใช้เทคโนโลยีที่เหมาะสมกับบทบาทของตนเองได้ง่ายขึ้น ทำให้สามารถปรับเปลี่ยนหรืออัปเกรดเทคโนโลยีในบาง Layer ได้โดยไม่กระทบกับทั้งระบบ
-   **ลดความซับซ้อนของ Deployment:** การแบ่งโครงสร้างเป็น Layer ช่วยให้การทดสอบและการตรวจสอบการเปลี่ยนแปลงก่อน Deployment มีความชัดเจนและง่ายขึ้น ลดโอกาสเกิดข้อผิดพลาดในการ Deploy

### ประโยชน์ที่ได้รับ:
-   **การแยกส่วนความรับผิดชอบ (Separation of Concerns):** แต่ละ Layer มีหน้าที่และความรับผิดชอบที่ชัดเจน ทำให้โครงสร้างโค้ดเป็นระเบียบ เข้าใจง่าย
-   **บำรุงรักษาง่ายขึ้น (Easier Maintainability):** การเปลี่ยนแปลงหรือแก้ไขข้อผิดพลาดใน Layer หนึ่งมักไม่ส่งผลกระทบต่อ Layer อื่น ทำให้การบำรุงรักษาและอัปเดตระบบทำได้ง่ายและรวดเร็ว
-   **เพิ่มความสามารถในการทดสอบ (Enhanced Testability):** แต่ละ Layer สามารถทดสอบแยกกันได้ (Unit Test, Integration Test) ทำให้มั่นใจได้ว่าแต่ละส่วนทำงานได้อย่างถูกต้อง
-   **ส่งเสริมการนำกลับมาใช้ใหม่ (Promotes Reusability):** คอมโพเนนต์ในแต่ละ Layer โดยเฉพาะ Business Logic หรือ Data Access สามารถนำกลับมาใช้ใหม่ในส่วนอื่นๆ ของแอปพลิเคชันหรือโปรเจกต์อื่นได้
-   **รองรับการพัฒนาแบบทีม (Facilitates Team Development):** ทีมพัฒนาหลายทีมสามารถทำงานพร้อมกันในแต่ละ Layer โดยมีการพึ่งพากันน้อยที่สุด ช่วยให้การพัฒนาเป็นไปอย่างรวดเร็วและมีประสิทธิภาพ

## 🚀 How to Run

```bash
# 1. Clone repository
git clone [your-repo-url]

# 2. Install dependencies
npm install

# 3. Run server
npm start

# 4. Test API
# Open browser: http://localhost:3000
```

## 📝 API Endpoints

The API is available under the base path `/api/students`.

-   **GET /api/students**: ดึงข้อมูลรายการนักเรียนทั้งหมด
-   **GET /api/students/:id**: ดึงข้อมูลนักเรียนคนเดียวตามรหัสประจำตัว
-   **POST /api/students**: สร้างนักเรียนใหม่
-   **PUT /api/students/:id**: อัปเดตรายละเอียดทั้งหมดของนักเรียนที่มีอยู่ตามรหัสประจำตัว
-   **PATCH /api/students/:id/gpa**: อัปเดตเกรดเฉลี่ย (GPA) ของนักเรียนตามรหัสประจำตัว
-   **PATCH /api/students/:id/status**: อัปเดตสถานะของนักเรียนตามรหัสประจำตัว
-   **DELETE /api/students/:id**: ลบนักเรียนตามรหัสประจำตัว